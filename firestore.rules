rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper function to check if user is authenticated
    // This allows both anonymous and signed-in users, which matches 
    // your app's use of firebase.auth().signInAnonymously()
    function isAuthenticated() {
      return request.auth != null;
    }

    // Rules for 'cases_v2' collection
    match /cases_v2/{caseId} {
      allow read, write: if isAuthenticated();
    }

    // Rules for 'promotions_v2' collection (uploads)
    match /promotions_v2/{promoId} {
      allow read, write: if isAuthenticated();
    }

    // Rules for 'tasks_v2' collection (dashboard tasks)
    match /tasks_v2/{taskId} {
      allow read, write: if isAuthenticated();
    }

    // Detailed Default Rule: Deny everything else by default
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
